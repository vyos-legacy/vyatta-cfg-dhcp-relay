type:  u32 
default:  10
help: Set to discard packets that have reached specified hop-count
syntax:expression: $VAR(@) >= 1 && $VAR(@) <= 255; "hop-count must be a value between 1 and 255"
comp_help:Possible completions:
  <1-255>           Set hop-count
